create database water;
use water;

create table register(username varchar(50) primary key,
password varchar(50),
contact_no varchar(20),
email varchar(70), 
address varchar(80));

create table techniques(username varchar(50), technique varchar(1500), filename varchar(50), upload_date varchar(30));

